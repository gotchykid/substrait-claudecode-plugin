# Runtime artifacts the SessionStart hook writes into the plugin root. They only appear
# here when someone tests this directory in place (`claude --plugin-dir ./substrait-plugin`);
# in a real install they live in ~/.claude/plugins/cache. Never commit or publish them.
.last-version-check
.portal-url
